============
Installation
============

At the command line::

    $ easy_install dbaas-zabbix

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dbaas-zabbix
    $ pip install dbaas-zabbix